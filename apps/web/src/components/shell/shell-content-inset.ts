/**
 * Horizontal + bottom padding for the main scroll region in `SidebarShellLayout`.
 * Routes that need content flush to the bottom of the viewport (e.g. full-height
 * lists) pass `flushBottom` on the shell instead of duplicating padding logic.
 */
export const SHELL_CONTENT_HORIZONTAL_PADDING_CLASS = "px-4 lg:px-6";

export const SHELL_CONTENT_BOTTOM_PADDING_CLASS = "pb-4 lg:pb-6";
