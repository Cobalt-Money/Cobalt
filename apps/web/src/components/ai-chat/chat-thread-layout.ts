/**
 * Shared horizontal bounds for the AI chat message list and composer.
 * Use this class on a wrapper that contains both so width stays in sync.
 */
export const CHAT_THREAD_COLUMN_CLASS = "mx-auto w-full max-w-[44rem]";
