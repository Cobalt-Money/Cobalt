// Placeholder registry. The boneyard-js CLI overwrites this file when you run
// `npx boneyard-js build`. Until then, every <Skeleton> falls back to its
// `fallback` prop (or renders nothing) while loading. Keep this file committed
// so the app boots on fresh clones.
export const __boneyardPlaceholder = true;
