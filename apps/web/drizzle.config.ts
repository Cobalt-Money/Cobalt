/**
 * Re-exports the shared Drizzle Kit config from `@cobalt-web/db`.
 * Run from `apps/web`: `bun run db:push`, `bun run db:generate`, etc.
 */
export { default } from "../../packages/db/drizzle.config.ts";
