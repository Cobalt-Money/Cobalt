# Landing background images

Hard-coded references in `apps/web/src/routes/index.tsx`:

- `mountains-bg.jpg` → `PREVIEW_SECTION_BG` (App Preview section)
- `oil-canvas.jpg` → `API_SECTION_BG` (Money as an API section)

To swap either: drop the new file here, update the constant in `index.tsx`.
