# Auth page video assets

Drop background loops for the `/login` split-screen here.

Expected:

- `<name>.mp4` — H.264, ~1080p+, silent
- `<name>-poster.jpg` — first-frame still (optional)

Reference in `apps/web/src/routes/login.tsx` via `/auth/videos/<file>`.
