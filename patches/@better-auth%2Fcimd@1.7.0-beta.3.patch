diff --git a/node_modules/@better-auth/cimd/.bun-tag-fd76e6ac15b00bf2 b/.bun-tag-fd76e6ac15b00bf2
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/index.mjs b/dist/index.mjs
index 73436bdcf7b3ebdb19ccd3f791f1f2438d5c74c0..d8a735c7e8739547e4c3f8ed85b1b713fa453d5b 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -161,14 +161,26 @@ function validateCimdMetadata(fetchUrl, raw, originBoundFields) {
 		valid: false,
 		error: "redirect_uris must be a non-empty array of absolute HTTP(S) URIs"
 	};
-	if (doc.grant_types !== void 0 && !(Array.isArray(doc.grant_types) && doc.grant_types.every((g) => typeof g === "string" && ALLOWED_GRANT_TYPES.has(g)))) return {
-		valid: false,
-		error: `grant_types must be a subset of [${[...ALLOWED_GRANT_TYPES].map((g) => `"${g}"`).join(", ")}]`
-	};
-	if (doc.response_types !== void 0 && !(Array.isArray(doc.response_types) && doc.response_types.every((r) => typeof r === "string" && ALLOWED_RESPONSE_TYPES.has(r)))) return {
-		valid: false,
-		error: "response_types must be a subset of [\"code\"]"
-	};
+	if (doc.grant_types !== void 0) {
+		if (!Array.isArray(doc.grant_types) || !doc.grant_types.every((g) => typeof g === "string")) return {
+			valid: false,
+			error: "grant_types must be an array of strings"
+		};
+		if (!doc.grant_types.includes("authorization_code")) return {
+			valid: false,
+			error: 'grant_types must include "authorization_code"'
+		};
+	}
+	if (doc.response_types !== void 0) {
+		if (!Array.isArray(doc.response_types) || !doc.response_types.every((r) => typeof r === "string")) return {
+			valid: false,
+			error: "response_types must be an array of strings"
+		};
+		if (!doc.response_types.includes("code")) return {
+			valid: false,
+			error: 'response_types must include "code"'
+		};
+	}
 	for (const field of ["client_uri", "logo_uri"]) {
 		if (doc[field] !== void 0 && typeof doc[field] !== "string") return {
 			valid: false,
@@ -332,6 +344,12 @@ const ALLOWED_METADATA_FIELDS = new Set([
 function toOAuthClientBody(metadata) {
 	const filtered = {};
 	for (const key of ALLOWED_METADATA_FIELDS) if (key in metadata) filtered[key] = metadata[key];
+	if (Array.isArray(filtered.grant_types)) {
+		filtered.grant_types = filtered.grant_types.filter((g) => typeof g === "string" && ALLOWED_GRANT_TYPES.has(g));
+	}
+	if (Array.isArray(filtered.response_types)) {
+		filtered.response_types = filtered.response_types.filter((r) => typeof r === "string" && ALLOWED_RESPONSE_TYPES.has(r));
+	}
 	return {
 		...filtered,
 		token_endpoint_auth_method: filtered.token_endpoint_auth_method ?? "none"
