import { defineQueries } from "@rocicorp/zero";

// Add your queries here using defineQuery
// Example:
//   myQuery: defineQuery(({ ctx }) => zql.myTable.where("userId", ctx.userId))

export const queries = defineQueries({});
